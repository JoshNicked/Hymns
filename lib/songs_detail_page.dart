import 'package:flutter/material.dart';

class SongsDetailPage extends StatelessWidget {
  final int index;
  const SongsDetailPage({super.key, required this.index});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Yo"),
      ),
    );
  }
}